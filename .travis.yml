sudo: required
services:
  - docker

before_install:
  - npm update -q
  - docker build -t antogcdev/docker-react -f Dockerfile.dev .

script:  #script should return 0 to success
  - docker run antogc/docker-react npm run test -- --coverage